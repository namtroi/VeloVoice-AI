// Session store tests stub — Phase 4
// TODO Phase 4: all state transitions, reconnect logic

import { describe, it } from 'vitest'

describe('useSessionStore', () => {
  it.todo('starts in idle state')
  it.todo('connect() transitions to connected')
  it.todo('disconnect() transitions to idle')
  it.todo('addMessage() appends to transcript')
  it.todo('setStatus() updates status')
  it.todo('setError() sets error string')
})
