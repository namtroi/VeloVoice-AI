// VAD controller stub — Phase 5
// TODO Phase 5: init @ricky0123/vad-web MicVAD, integrate with audio-capture, update SessionStore state

export class VadController {
  async start(
    _onSpeechStart: () => void,
    _onSpeechEnd: () => void,
  ): Promise<void> {
    // stub
  }

  stop(): void {
    // stub
  }
}
